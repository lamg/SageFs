<DataTemplate xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
              xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
              xmlns:vs="http://schemas.microsoft.com/visualstudio/extensibility/2022/xaml"
              xmlns:colors="clr-namespace:Microsoft.VisualStudio.PlatformUI;assembly=Microsoft.VisualStudio.Shell.15.0"
              xmlns:styles="clr-namespace:Microsoft.VisualStudio.Shell;assembly=Microsoft.VisualStudio.Shell.15.0">
  <Border Background="{DynamicResource {x:Static colors:EnvironmentColors.ToolWindowBackgroundBrushKey}}"
          TextElement.Foreground="{DynamicResource {x:Static colors:EnvironmentColors.ToolWindowTextBrushKey}}"
          Padding="8">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
      <StackPanel>
        <!-- Refresh button + connection status -->
        <DockPanel Margin="0,0,0,8">
          <Button Content="↻ Refresh"
                  Command="{Binding RefreshCommand}"
                  DockPanel.Dock="Left"
                  MinHeight="24" MinWidth="80"
                  Style="{StaticResource {x:Static styles:VsResourceKeys.ButtonStyleKey}}" />
          <TextBlock Text="{Binding ConnectionStatus}"
                     DockPanel.Dock="Right"
                     VerticalAlignment="Center"
                     FontFamily="Cascadia Code,Consolas,Courier New"
                     FontSize="12" FontWeight="Bold"
                     HorizontalAlignment="Right"
                     Margin="8,0,0,0" />
        </DockPanel>

        <!-- Session info -->
        <TextBlock Text="{Binding SessionInfo}"
                   FontFamily="Cascadia Code,Consolas,Courier New"
                   FontSize="12"
                   TextWrapping="Wrap"
                   Margin="0,0,0,12" />

        <!-- Assemblies -->
        <TextBlock Text="{Binding AssembliesInfo}"
                   FontFamily="Cascadia Code,Consolas,Courier New"
                   FontSize="11"
                   TextWrapping="Wrap"
                   Margin="0,0,0,12" />

        <!-- Namespaces -->
        <TextBlock Text="{Binding NamespacesInfo}"
                   FontFamily="Cascadia Code,Consolas,Courier New"
                   FontSize="11"
                   TextWrapping="Wrap"
                   Margin="0,0,0,12" />

        <!-- Failed opens -->
        <TextBlock Text="{Binding FailedOpensInfo}"
                   FontFamily="Cascadia Code,Consolas,Courier New"
                   FontSize="11"
                   Foreground="OrangeRed"
                   TextWrapping="Wrap"
                   Margin="0,0,0,12" />

        <!-- Hot reload -->
        <TextBlock Text="{Binding HotReloadInfo}"
                   FontFamily="Cascadia Code,Consolas,Courier New"
                   FontSize="11"
                   TextWrapping="Wrap"
                   Margin="0,0,0,8" />
      </StackPanel>
    </ScrollViewer>
  </Border>
</DataTemplate>
